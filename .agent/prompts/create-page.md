Create a new static HTML example page.

Use the NHSW design system rules from CLAUDE.md.

Page request:
[DESCRIBE PAGE HERE]

Requirements:
- Use existing NHSW components only.
- Use accessible form patterns.
- Use the site header, main wrapper and footer.
- Add the page under preview/examples/.
- Add a link to preview/index.html.
- Run the Sass build afterwards.
